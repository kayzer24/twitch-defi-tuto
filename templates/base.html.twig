<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Twitch Défi{% endblock %}</title>

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {{ include('components/header.html.twig') }}
        <main>
            {% block body %}{% endblock %}
        </main>
        {{ include('components/footer.html.twig') }}
    </body>
</html>
